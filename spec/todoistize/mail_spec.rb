require 'spec_helper'

describe Todoistize::Mail do
  it 'has a version number' do
    expect(Todoistize::Mail::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
